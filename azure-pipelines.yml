trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- script: |
    echo "Enterprise DevSecOps Pipeline Started"
    python --version
  displayName: 'Validation'

- task: Docker@2
  displayName: Build Docker Image
  inputs:
    command: build
    Dockerfile: app/Dockerfile
    repository: devsecops-app
